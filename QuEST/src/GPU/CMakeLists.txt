#set(QuEST_SRC_ARCHITECTURE_DEPENDENT
#    ${CMAKE_CURRENT_SOURCE_DIR}/QuEST_gpu.cu
#    PARENT_SCOPE
#)

if (UNIMEM)
    set(QuEST_SRC_ARCHITECTURE_DEPENDENT
        ${CMAKE_CURRENT_SOURCE_DIR}/QuEST_gpu_unified.cu
        PARENT_SCOPE
    )
elseif(MANUAL)
    set(QuEST_SRC_ARCHITECTURE_DEPENDENT
        ${CMAKE_CURRENT_SOURCE_DIR}/QuEST_gpu_manual.cu
        PARENT_SCOPE
    )
else()
    set(QuEST_SRC_ARCHITECTURE_DEPENDENT
    ${CMAKE_CURRENT_SOURCE_DIR}/QuEST_gpu.cu
    PARENT_SCOPE
    )
endif()


